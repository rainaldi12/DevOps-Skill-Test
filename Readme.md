# 🧩 SkillTest DevOps — All Docker Containers

This repository contains a complete DevOps environment for a **PHP Yii2 web application**, **PostgreSQL database**, **Nginx reverse proxy**, and **Monitoring stack** with **Prometheus** and **Grafana**.  
The setup focuses on modularity, simplicity, and production-aligned best practices — ideal for technical evaluations.

---

## 🚀 How to Setup

### Prerequisites
Ensure the following are installed and running:
- Docker Engine ≥ 24.x
- Docker Compose Plugin ≥ 2.x
- Bash / WSL / Linux Terminal

### Quick Start
Simply run the bootstrap script:

```bash
sh easy-on.sh
```

## How to destroy all container setup

### Quick destroy
simply run bash script :

```bash
sh easy-down.sh
```
